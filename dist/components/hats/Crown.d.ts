import React from 'react';
import { ClothingProps } from '../clothing/types';
import { HatProps } from './types';
export declare const Front: ({ color, scale }: ClothingProps & HatProps) => React.JSX.Element;
export declare const Back: ({ scale }: HatProps) => React.JSX.Element;
